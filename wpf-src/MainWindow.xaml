<Window x:Class="TimberFlowDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TimberFlow ERP" Width="1500" Height="920"
        MinWidth="1180" MinHeight="720"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        Background="#F1F5F9" FontFamily="Segoe UI" FontSize="12"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="256"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= SIDEBAR ================= -->
        <Border Grid.Column="0" Background="{StaticResource SideBg}"
                BorderBrush="{StaticResource SideBorder}" BorderThickness="0,0,1,0">
            <DockPanel>
                <!-- Logo & tiêu đề -->
                <Border DockPanel.Dock="Top" Padding="24,22" BorderBrush="#991E293B" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="32" Height="32" Background="{StaticResource Blue500}" CornerRadius="4">
                            <TextBlock Text="TF" Foreground="White" FontWeight="Bold" FontSize="13"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="TimberFlow ERP" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                            <TextBlock Text="V1.0.4 - PRO EDITION" Foreground="#7A94A3B8" FontSize="9"
                                       Margin="0,3,0,0">
                                <TextBlock.LayoutTransform>
                                    <ScaleTransform/>
                                </TextBlock.LayoutTransform>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Chân sidebar: thông tin hệ thống -->
                <Border DockPanel.Dock="Bottom" Padding="16,14" Background="#33020617"
                        BorderBrush="#991E293B" BorderThickness="0,1,0,0">
                    <StackPanel>
                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="Người dùng:" Foreground="{StaticResource Slate500}" FontSize="10"/>
                            <TextBlock Text="Admin (Single-User)" Foreground="{StaticResource Slate400}"
                                       FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Database:" Foreground="{StaticResource Slate500}" FontSize="10"/>
                            <TextBlock Text="SQLite (Local)" Foreground="{StaticResource Emerald500}"
                                       FontFamily="{StaticResource FontMono}" FontSize="10" FontWeight="SemiBold"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Menu điều hướng -->
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12,0">
                    <StackPanel x:Name="NavPanel" Margin="0,12,0,12">
                        <TextBlock Text="PHÂN HỆ KHO &amp; MUA HÀNG" Foreground="{StaticResource Slate500}"
                                   FontSize="10.5" FontWeight="Bold" Margin="12,16,12,8"/>
                        <!-- Các mục menu được sinh trong code-behind -->
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- ================= VÙNG LÀM VIỆC ================= -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="24"/>
            </Grid.RowDefinitions>

            <!-- Dải tab động -->
            <Border Grid.Row="0" Background="#EDF1F6" BorderBrush="{StaticResource Slate200}"
                    BorderThickness="0,0,0,1" Padding="16,0,0,0">
                <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="TabStrip" Orientation="Horizontal"/>
                </ScrollViewer>
            </Border>

            <!-- Header / breadcrumb -->
            <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource Slate200}"
                    BorderThickness="0,0,0,1" Padding="24,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Home" Foreground="{StaticResource Slate500}" FontSize="13" FontWeight="Medium"/>
                        <TextBlock Text="/" Foreground="{StaticResource Slate300}" FontSize="13" Margin="8,0"/>
                        <TextBlock Text="TimberFlow ERP" Foreground="{StaticResource Slate900}" FontSize="13" FontWeight="Medium"/>
                        <TextBlock Text="/" Foreground="{StaticResource Slate300}" FontSize="13" Margin="8,0"/>
                        <TextBlock x:Name="BreadcrumbCurrent" Text="Bảng Điều Khiển"
                                   Foreground="{StaticResource Slate900}" FontSize="13" FontWeight="SemiBold"
                                   MouseLeftButtonDown="BreadcrumbCurrent_Click"/>
                        <TextBlock x:Name="BreadcrumbSep2" Text="/" Foreground="{StaticResource Slate300}"
                                   FontSize="13" Margin="8,0" Visibility="Collapsed"/>
                        <TextBlock x:Name="BreadcrumbDetail" Foreground="{StaticResource Slate900}"
                                   FontSize="13" FontWeight="SemiBold" Visibility="Collapsed"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="{StaticResource Slate50}"
                            BorderBrush="{StaticResource Slate200}" BorderThickness="1" CornerRadius="4" Padding="12,5">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="PulseDot" Width="6" Height="6" Fill="{StaticResource Emerald500}"
                                     VerticalAlignment="Center"/>
                            <TextBlock Text="Hệ Thống Sẵn Sàng" Foreground="{StaticResource Slate600}"
                                       FontSize="12" FontWeight="Medium" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Nội dung màn hình -->
            <Border Grid.Row="2" Background="{StaticResource Slate50}">
                <ContentControl x:Name="ContentHost"/>
            </Border>

            <!-- Thanh trạng thái dưới cùng -->
            <Border Grid.Row="3" Background="{StaticResource Blue600}" Padding="16,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="DB: SQLITE CONNECTED" Foreground="White" FontSize="9" FontWeight="Bold"/>
                        <TextBlock Text="SERVER: LOCAL DESKTOP" Foreground="White" FontSize="9" FontWeight="Bold" Margin="16,0,0,0"/>
                        <TextBlock Text="MVVM CORE READY" Foreground="{StaticResource Blue200}" FontSize="9" FontWeight="Bold" Margin="16,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="PHÂN XƯỞNG SẢN XUẤT GỖ #1" Foreground="White" Opacity="0.7" FontSize="9" FontWeight="Bold"/>
                        <TextBlock Text="QUYỀN HẠN: ARCHITECT_ADMIN" Foreground="White" FontSize="9" FontWeight="Bold" Margin="12,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
